structure (Results) {
  description (전체 결과값을 가지고 있는 구조체)
  property (roadAddr) {
    type (RoadAddress)
    min (Required) max (One)
  }
  property (engAddr){
    type (EngAddress)
    min(Required) max(One)
  }
  property (zipCode){
    type(ZipCode)
    min(Required) max(One)
  }
}
